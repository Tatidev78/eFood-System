import styles from 'styled-components';

import { cores } from '../../styles';

export const Container = styles.div`
width: 345px; 
height: 347px; 
top: 32px; 
left: 1013px;

background: ${cores.fundoTag};
`;